class Player {
	playTurn(warrior) {
    var func = {
      enemyAround: function() {
      }
    };

	  if (func.enemyAround()) {
      warrior.bind();
    } else if () {
      warrior.detonate();
    } else {
      warrior.walk('left');
    }

  }
}

